# AI Prompt to Recreate Registration Form

To recreate the registration form exactly as developed, use the following prompt:

---

**Prompt:**

Create a single HTML file named `rrr.html` that contains a simple registration form.

**Constraints:**
- Use **ONLY plain HTML**.
- Strictly **NO CSS** (inline, internal, or external).
- Strictly **NO Bootstrap** or any other styling frameworks.

**Form Requirements:**
1.  **Heading:** `<h1>Registration Form</h1>`.
2.  **Fields:**
    - **Email Id:** A text input with a preceding label.
    - **Mobile Number:** A text input with a preceding label.
    - **Country:** A dropdown (`<select>`) containing:
        - `--Select Country--` (default)
        - `India`
        - `USA`
    - **State:** A dropdown (`<select>`) containing the following options:
        - `--Select State--` (default)
        - `Andhra Pradesh`
        - `Telangana`
        - `Karnataka`
        - `Tamil Nadu`
        - `Maharashtra`
        - `Kerala`
        - `California`
        - `Texas`
        - `New York`
        - `Florida`
        - `Illinois`
        - `Washington`
    - **Terms & Conditions:** A checkbox followed by the text `Please Accept Terms & Conditions`.
3.  **Action:** A button labeled `Register` at the bottom of the form.
4.  **Layout:** Use `<br>` tags to separate labels from inputs and to create spacing between fields.

---
